<h1>商品詳細</h1>

<table>
  <thead>
    <tr>
      <th>商品名</th>
      <td><%= item.name %></td>
    </tr>
    <tr>
      <th>商品説明</th>
      <td><%= item.introduce %></td>
    </tr>
    <tr>
      <th>ジャンル</th>
      <td><%= item.genre_name %></td>
    </tr>
      <th>税抜価格(税込価格)</th>
      <td>
        <%= item.price %>
        <!--<税込価格表示できるようにする>-->
      </td>
    </tr>
    <tr>
      <th>販売ステータス</th>
      <td><%= item.is_active %></td>
    </tr>
    <tr>
      <%= button_to "編集する", edit_admin_item_path(customer.id) %>
    </tr>
  </thead>
</table>